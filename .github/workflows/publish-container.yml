name: Node.js Package
on: [push, pull_request]

jobs:
  build-container:
    runs-on: ubuntu-latest
    steps:

    # checkout the repo
    - uses: actions/checkout@master

    - uses: azure/container-actions/docker-login@master
      with:
        username: ${{ secrets.REGISTRY_USERNAME }}
        password: ${{ secrets.REGISTRY_PASSWORD }}

    - run: |
      docker build . -t dmckinstry/contosoair:latest -t:dmckinstry/contosoair:${{ github.sha }}
      docker push dmckinstry/contosoair:latest
      docker push dmckinstry/contosoair:${{ github.sha }}

#    - uses: azure/login@v1
#      with:
#        creds: ${{ secrets.AZURE_CREDENTIALS }}

#    - name: 'Run Azure webapp deploy action using publish profile credentials'
#      uses: azure/webapps-deploy@v1
#      with: 
#         app-name: davepfx1234-web-dev # Replace with your app name
        #package: '.'  #default value is entire repo
        # If your function app project is not located in your repository's root
        # Please consider prefixing the project path in this package parameter

 
